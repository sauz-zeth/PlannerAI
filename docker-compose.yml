services:
  ai-planner:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app  # Добавьте эту строку
    environment:
      - PYTHONPATH=/app
      - ENVIRONMENT=development
    restart: unless-stopped